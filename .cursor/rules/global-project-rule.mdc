---
alwaysApply: true
---

The project writes a lightweight C wrapper around the c library coco, in coco.h and coco.c
We dont use RCPP.

Respect the .lintr file for the generated R code.

Never delete files without asking, never install software without asking.
Never anything outside the current project directory.

@.lintr


